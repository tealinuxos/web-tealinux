---
const { variant = "default", type = "button", ...rest } = Astro.props
---

<button 
  type={type} 
  class:list={[
    "px-3.5 py-1.5 rounded-md text-sm cursor-pointer border-none transition-colors",
    variant === "default" && "bg-slate-700 text-white hover:bg-slate-600",
    variant === "danger" && "bg-red-500 text-white hover:bg-red-600"
  ]}
  {...rest}
>
  <slot />
</button>
